services:
  server:
    build: .
    ports:
      - "5001:5000"
    env_file:
      - .env
    depends_on:
      - mongo

  mongo:
    image: mongo:8
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: